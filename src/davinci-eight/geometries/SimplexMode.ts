/**
 * The common low values for a Simplex.
 */
export enum SimplexMode {
    EMPTY = -1,
    POINT = 0,
    LINE = 1,
    TRIANGLE = 2,
    TETRAHEDRON = 3,
    FIVE_CELL = 4,
}

export default SimplexMode;
