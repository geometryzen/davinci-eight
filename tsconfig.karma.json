{
    "extends": "./tsconfig",
    "compilerOptions": {
        "outDir": "karma_test_build",
        "module": "CommonJS",
        "declaration": false
    },
    "exclude": [
        // We don't exclude the specification files when testing.
    ]
}