{
    "extends": "./tsconfig",
    "compilerOptions": {
        "outDir": "test",
        "module": "CommonJS",
        "declaration": false
    },
    "exclude": [
        // We don't exclude the specification files when testing.
    ]
}