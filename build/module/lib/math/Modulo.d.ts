/**
 * Modulo Arithmetic (Experimental).
 */
export declare class Modulo {
    private _value;
    private _size;
    get size(): number;
    set size(size: number);
    get value(): number;
    set value(value: number);
    inc(): number;
}
